---
import { getCollection, render } from 'astro:content';
import '../styles/global.css';

const concepts = await getCollection('concepts');
concepts.sort((a, b) => {
  const ratingA = a.data.rating || 0;
  const ratingB = b.data.rating || 0;
  if (ratingB !== ratingA) return ratingB - ratingA;
  return new Date(b.data.date_added).getTime() - new Date(a.data.date_added).getTime();
});

const renderedConcepts = await Promise.all(
  concepts.map(async (entry) => {
    const { Content } = await render(entry);
    return { entry, Content };
  })
);
---

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atlas Field Notes - Concepts</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="">
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Inter:wght@400;500;600;700&family=Reenie+Beanie&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #FDFCF9;
            background-image: 
                url("https://www.transparenttextures.com/patterns/natural-paper.png"),
                url("https://www.transparenttextures.com/patterns/natural-paper.png"),
                url("https://www.transparenttextures.com/patterns/natural-paper.png");
            background-position: 0px 0px, 40px 60px, 80px 20px;
            background-repeat: repeat;
            color: #2A2724;
        }
        .hand-underline-sm { position: relative; display: inline-block; }
        .catalog-card {
            background: white !important;
            transition: all 0.3s ease;
            cursor: pointer;
            position: relative;
        }
        .catalog-card:hover {
            background: #F5F2EB !important;
            border-color: #D5CCB6;
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -10px rgba(42, 39, 36, 0.1);
        }
        .catalog-card:hover .model-title { color: #8B2626; }
        .bookmark-icon {
            position: absolute;
            top: 1.5rem;
            right: 1.5rem;
            color: #A39985;
            transition: color 0.2s ease;
            z-index: 10;
        }
        .catalog-card:hover .bookmark-icon { color: #8B2626; }
        .star-rating { display: flex; gap: 4px; color: #8B2626; }
        .masonry-grid {
            position: relative;
            width: 100%;
            transition: height 0.4s ease;
        }
        .masonry-item-wrapper {
            position: absolute;
            transition: transform 0.6s cubic-bezier(0.2, 0.8, 0.2, 1), opacity 0.4s ease, width 0.4s ease;
            padding: 0;
            margin: 0;
        }
        .masonry-item { 
            break-inside: avoid; 
            margin-bottom: 1.25rem; 
            display: block; 
            position: relative;
        }
        .glass-nav { backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); }
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background-color: rgba(42, 39, 36, 0.12); border-radius: 10px; border: 2px solid transparent; background-clip: content-box; }
        ::-webkit-scrollbar-thumb:hover { background-color: rgba(42, 39, 36, 0.25); }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        @keyframes slideUpScale { from { opacity: 0; transform: translateY(40px) scale(0.97); } to { opacity: 1; transform: translateY(0) scale(1); } }
        @keyframes slideInRight { from { opacity: 0; transform: translateX(-40px); } to { opacity: 1; transform: translateX(0); } }
        @keyframes slideUpStagger { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .animate-fade-in { animation: fadeIn 0.3s ease-out forwards; }
        .animate-modal-enter { opacity: 0; animation: slideUpScale 0.5s cubic-bezier(0.16, 1, 0.3, 1) 0.05s forwards; }
        .animate-content-enter { opacity: 0; animation: slideInRight 0.5s cubic-bezier(0.2, 0.8, 0.2, 1) 0.15s forwards; }
        .animate-sidebar-item { opacity: 0; animation: slideUpStagger 0.4s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; }
        .stagger-1 { animation-delay: 0.2s; } .stagger-2 { animation-delay: 0.28s; } .stagger-3 { animation-delay: 0.36s; }
        .stagger-4 { animation-delay: 0.44s; } .stagger-5 { animation-delay: 0.52s; } .stagger-6 { animation-delay: 0.6s; } .stagger-7 { animation-delay: 0.68s; }
        .ink-stain {
            background-color: #FDFCF9;
            background-image: url("https://www.transparenttextures.com/patterns/natural-paper.png"), url("https://www.transparenttextures.com/patterns/natural-paper.png"), radial-gradient(circle at 10% 90%, rgba(42,39,36,0.03) 0%, rgba(253,252,249,0) 60%);
            background-position: 0px 0px, 40px 60px, 0 0;
            background-repeat: repeat, repeat, no-repeat;
        }
        .swoosh-path { stroke-dasharray: 100; stroke-dashoffset: 100; }
        @keyframes drawSwoosh { to { stroke-dashoffset: 0; } }
        .animate-draw-line { animation: drawSwoosh 0.8s ease-out 0.4s forwards; }
        .hatched-pattern { background-image: repeating-linear-gradient(45deg, #2A2724 0, #2A2724 1px, transparent 0, transparent 8px); }
        .custom-scrollbar-modal::-webkit-scrollbar-thumb { background-color: rgba(42, 39, 36, 0.15); border-radius: 10px; }
        .modal-scrollbar::-webkit-scrollbar { width: 4px; }
        .modal-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .modal-scrollbar::-webkit-scrollbar-thumb { background-color: rgba(42, 39, 36, 0.15); border-radius: 10px; }
    </style>
</head>
<body class="min-h-screen antialiased selection:bg-[#8B2626] selection:text-white flex flex-col">

    <header class="sticky top-0 z-50 w-full h-[80px] bg-[#F5F2EB]/95 glass-nav flex items-center justify-between px-10 shadow-sm" style="border-bottom: 0.5px solid rgba(42, 39, 35, 0.12);">
        <div class="flex items-center gap-10">
            <a href="/astro-project-v2/" class="font-serif text-xl text-[#A39985] transition-all duration-300 hover:text-[#2A2724] hover:-translate-y-[1px]">Main Page</a>
            <div class="h-8 w-px bg-[#E8E2D2]"></div>
            <nav class="flex items-center gap-8 translate-y-[2px]">
                <a href="/astro-project-v2/concepts" class="group flex items-center gap-2 transition-all duration-300 hover:-translate-y-[1px]">
                    <span class="w-1.5 h-1.5 rounded-full bg-[#8B2626] shadow-[0_0_8px_rgba(139,38,38,0.4)]"></span>
                    <span class="font-serif text-xl text-[#2A2724]">Concepts</span>
                </a>
                <a href="/astro-project-v2/definitions" class="font-serif text-xl text-[#A39985] transition-all duration-300 hover:text-[#2A2724] hover:-translate-y-[1px]">Definitions</a>
                <a href="Grid Frameworks Main.html" class="font-serif text-xl text-[#A39985] transition-all duration-300 hover:text-[#2A2724] hover:-translate-y-[1px]">Frameworks</a>
            </nav>
        </div>
        <div class="font-['Reenie_Beanie'] text-[1.75rem] leading-none text-seal tracking-tight translate-y-1">hyperlynxt | teo</div>
    </header>

    <main class="flex-1 pb-32">
        <section class="w-full bg-gradient-to-b from-[#F5F2EB] to-transparent pt-20 pb-16 px-6">
            <div class="max-w-4xl mx-auto text-center">
                <span class="font-['Reenie_Beanie'] text-2xl text-[#8C8070] mb-2 block">A catalog of known things</span>
                <h2 class="font-serif text-5xl md:text-7xl leading-tight text-[#2A2724] mb-8">
                    Collected <span class="italic hand-underline-sm inline-block">
                        Concepts
                        <svg class="absolute w-[110%] h-[12px] -bottom-1 -left-[5%] text-[#8B2626] opacity-70 overflow-visible" viewBox="0 0 100 10" fill="none" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
                            <path d="M0,8 C25,12 75,4 100,8" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="swoosh-path animate-draw-line"></path>
                        </svg>
                    </span>
                </h2>
                <p class="font-serif italic text-lg md:text-xl text-[#8C8070] max-w-2xl mx-auto leading-relaxed">
                    Concepts for making sense of thought, behavior, and the self.
                </p>
            </div>
        </section>

        <div class="max-w-7xl mx-auto px-6">
            <div class="masonry-grid">
                {renderedConcepts.map(({ entry, Content }) => (
                    <div class="masonry-item-wrapper">
                        <article
                            class="masonry-item catalog-card group bg-white rounded-3xl border border-[#E8E2D2] p-8 flex flex-col gap-6 transition-all duration-500 hover:shadow-[0_20px_40px_-15px_rgba(213,204,182,0.5)]"
                            data-title={entry.data.title}
                            data-short={entry.data.short_description}
                            data-id={entry.id}
                            data-discipline={entry.data.discipline}
                            data-tags={entry.data.tags.join(',')}
                            data-origin={entry.data.origin}
                            data-rating={entry.data.rating}
                            data-linked={entry.data.linked_notes}
                            data-fieldnote={entry.data.field_note || ''}
                            data-notesofused={entry.data.notes_of_used ? entry.data.notes_of_used.join(',') : ''}
                            data-related={entry.data.related_concepts ? entry.data.related_concepts.join(',') : ''}
                            data-authors={entry.data.authors ? entry.data.authors.join(',') : ''} 
                        >
                            <svg class="bookmark-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"></path></svg>
                            <div class="flex flex-col gap-2">
                                <h3 class="model-title font-serif text-3xl font-medium text-[#2A2724] transition-colors leading-tight">{entry.data.title}</h3>
                                <p class="font-serif text-[#8C8070] text-[16px] leading-relaxed mt-2">{entry.data.short_description}</p>
                            </div>
                            <div class="pt-4 border-t border-[#F5F2EB] mt-auto flex justify-between items-center">
                                <div class="star-rating">
                                    {Array.from({ length: entry.data.rating }).map((_, i) => (
                                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round" style={i === 1 ? "transform: rotate(15deg);" : i === 2 ? "transform: rotate(-10deg);" : i === 3 ? "transform: rotate(5deg);" : i === 4 ? "transform: rotate(-15deg);" : ""}>
                                            <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" fill="currentColor" fill-opacity="0.2"></path>
                                        </svg>
                                    ))}
                                </div>
                                <span class="font-['Reenie_Beanie'] text-[26px] text-[#8B2626] translate-y-1">{entry.data.linked_notes} times used</span>
                            </div>
                        </article>
                    </div>
                ))}
            </div>
        </div>
    </main>

    <style>
        /* Star Filter Fixes */
        .star-filter { transition: all 0.25s ease; cursor: pointer; display: flex; align-items: center; justify-content: center; }
        .star-filter.active { color: #8B2626 !important; opacity: 1 !important; }
        .star-filter.active svg { color: #8B2626 !important; }
        .star-filter.active svg path { fill: #8B2626 !important; fill-opacity: 0.2 !important; stroke: #8B2626 !important; stroke-width: 1.5 !important; }
        .star-filter:not(.active) { color: #A39985 !important; opacity: 0.5 !important; }
        .star-filter:not(.active) svg { color: #A39985 !important; }
        .star-filter:not(.active) svg path { fill: #A39985 !important; fill-opacity: 0.1 !important; stroke: #A39985 !important; stroke-width: 1.5 !important; }
        .star-filter:hover { transform: scale(1.1); }
    </style>

    <div style="display:none;" aria-hidden="true">
        {renderedConcepts.map(({ entry, Content }) => (
            <div id={`content-${entry.id}`} class="concept-body prose font-serif text-xl leading-[1.85] text-[#2A2724]/85 space-y-8 max-w-[850px]">
                <Content />
            </div>
        ))}
    </div>
    </main>

    <!-- Concept Modal -->
    <div id="concept-modal" style="display:none;" class="fixed inset-0 z-[100] bg-[#2A2724]/25 backdrop-blur-[5px] items-center justify-center p-6 md:p-10 animate-fade-in">
        <div class="w-full max-w-[1240px] h-[92vh] bg-white rounded-[40px] shadow-[0_40px_100px_rgba(42,39,36,0.15)] overflow-hidden border border-white/60 animate-modal-enter relative ring-1 ring-[#D5CCB6]/30">
            <!-- Content Area -->
            <div class="h-full flex flex-col relative bg-[#FDFCF9] ink-stain custom-scrollbar-modal overflow-y-auto z-10">
                <!-- Close Button -->
                <div class="absolute top-0 right-0 p-8 flex justify-end z-[60] pointer-events-none">
                    <button id="close-modal-btn" class="w-12 h-12 rounded-full bg-white/80 backdrop-blur-md border border-[#D5CCB6] shadow-sm hover:bg-white flex items-center justify-center text-[#2A2724] transition-all group pointer-events-auto">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 opacity-60 group-hover:opacity-100 group-hover:rotate-90 transition-all duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path></svg>
                    </button>
                </div>

                <div class="px-10 md:px-20 lg:px-24 pb-32 pt-8 relative animate-content-enter">
                    <div id="modal-bg-letter" class="absolute top-10 right-16 text-[32rem] font-serif font-bold text-[#2A2724]/[0.015] leading-none pointer-events-none select-none -z-10 -rotate-3"></div>
                    <div class="mb-10">
                        <h1 class="font-serif text-5xl md:text-6xl lg:text-[5.5rem] font-medium text-[#2A2724] leading-[1.01] mb-6 tracking-tight relative">
                            <span id="modal-title" class="italic inline"></span>
                            <svg id="modal-title-underline" class="absolute h-[12px] text-[#8B2626] opacity-70 overflow-visible pointer-events-none" viewBox="0 0 100 10" fill="none" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none" style="display: none;">
                                <path d="M0,8 C25,12 75,4 100,8" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="swoosh-path"></path>
                            </svg>
                        </h1>
                        <div id="modal-stars" class="flex gap-2 items-center text-[#B08D57] mb-6"></div>

                        <!-- Header Ontology Section -->
                        <div id="modal-ontology-attribution" class="font-serif italic text-[1.25rem] mb-6 flex flex-wrap items-baseline gap-x-8 gap-y-2" style="display:none;">
                            <div id="modal-discipline-container" class="flex items-baseline gap-2">
                                <span style="color:#8B2626;opacity:0.8;font-variant:small-caps;letter-spacing:0.1em;font-size:0.9rem;font-weight:700;font-style:normal;">Discipline</span>
                                <span id="modal-discipline-text" style="color:#B08D57;opacity:0.7;font-style:italic;"></span>
                            </div>
                            <div id="modal-framework-container" class="flex items-baseline gap-2" style="display:none;">
                                <span style="color:#8B2626;opacity:0.8;font-variant:small-caps;letter-spacing:0.1em;font-size:0.9rem;font-weight:700;font-style:normal;">Parent Framework</span>
                                <span id="modal-framework-text" style="color:#B08D57;opacity:0.7;font-style:italic;"></span>
                            </div>
                        </div>

                        <div id="modal-attribution" class="font-serif italic text-[1.35rem] mb-6 flex items-baseline gap-2" style="display:none;">
                            <span style="color:#8B2626;opacity:0.8;font-variant:small-caps;letter-spacing:0.1em;font-size:0.9rem;font-weight:700;font-style:normal;">From the work of</span>
                            <span id="modal-authors-list" style="color:#B08D57;opacity:0.7;font-style:italic;"></span>
                        </div>
                        <div id="modal-tags-attribution" class="font-serif italic text-[1.25rem] mb-12 flex items-baseline gap-2" style="display:none;">
                            <span style="color:#8B2626;opacity:0.8;font-variant:small-caps;letter-spacing:0.1em;font-size:0.9rem;font-weight:700;font-style:normal;">Tags</span>
                            <span id="modal-tags-list" style="color:#B08D57;opacity:0.7;font-style:italic;"></span>
                        </div>
                        <div class="font-serif text-[1.25rem] leading-[1.65] text-[#2A2724] pl-6 border-l-[3px] border-[#B08D57]/40 max-w-4xl">
                            <p id="modal-summary"></p>
                        </div>
                    </div>

                    <div id="modal-detailed-desc" class="font-serif text-xl md:text-[1.45rem] leading-[1.85] text-[#2A2724]/85 space-y-8 max-w-[900px] z-10 relative mb-20"></div>

                    <!-- Notes of Myself Section (Replaces Field Note Container) -->
                    <div id="modal-field-note-section" class="mb-12 max-w-[900px]" style="display:none;">
                        <div style="color:#8B2626;opacity:0.8;font-variant:small-caps;letter-spacing:0.1em;font-size:0.8rem;font-weight:700;font-style:normal;margin-bottom:0.5rem;">Notes of Myself</div>
                        <p id="modal-field-note" class="font-['Reenie_Beanie'] text-2xl text-[#2A2724]/90"></p>
                    </div>

                    <!-- Metadata Grid -->
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-[1000px]">
                        <!-- Ontology -->

                        <!-- Related Concepts -->
                        <div id="modal-related-section" class="md:col-span-2 bg-white rounded-3xl border border-[#D5CCB6]/40 p-8 shadow-sm" style="display:none;">
                            <h3 class="font-sans text-[11px] font-bold uppercase tracking-[0.2em] text-[#2A2724]/40 mb-5 flex items-center gap-2.5">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#8B2626]" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path></svg>
                                Related Concepts
                            </h3>
                            <div id="modal-related-items" class="grid grid-cols-1 md:grid-cols-2 gap-3"></div>
                        </div>

                        <!-- Origin -->
                        <div class="bg-white rounded-3xl border border-[#D5CCB6]/40 p-8 shadow-sm">
                            <h3 class="font-sans text-[11px] font-bold uppercase tracking-[0.2em] text-[#2A2724]/40 mb-4 flex items-center gap-2.5">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#8B2626]" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 21v-4m0 0V5a2 2 0 012-2h6.5l1 1H21l-3 6 3 6h-8.5l-1-1H5a2 2 0 00-2 2zm9-13.5V9"></path></svg>
                                Origin
                            </h3>
                            <p id="modal-origin" class="font-serif text-[1.1rem] leading-relaxed text-[#2A2724]/70 italic"></p>
                        </div>

                        <!-- Utility & Usage -->
                        <div class="bg-white rounded-3xl border border-[#D5CCB6]/40 p-8 shadow-sm">
                            <h3 class="font-sans text-[11px] font-bold uppercase tracking-[0.2em] text-[#2A2724]/40 mb-5 flex items-center gap-2.5">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#8B2626]" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path></svg>
                                Linked Insights
                            </h3>
                            <div class="flex items-baseline gap-4">
                                <div id="modal-linked" class="text-6xl font-serif text-[#2A2724] tracking-tight"></div>
                                <div class="text-[12px] font-sans text-[#2A2724]/40 uppercase tracking-widest font-bold leading-tight">Total<br>Times Used</div>
                            </div>
                        </div>

                        <!-- Notes of Used -->
                        <div id="modal-notes-section" class="md:col-span-2 bg-white rounded-3xl border border-[#D5CCB6]/40 p-8 shadow-sm" style="display:none;">
                            <h3 class="font-sans text-[11px] font-bold uppercase tracking-[0.2em] text-[#2A2724]/40 mb-5 flex items-center gap-2.5">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-[#8B2626]" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 002-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path></svg>
                                Notes of Used
                            </h3>
                            <div id="modal-notes-items" class="grid grid-cols-1 md:grid-cols-2 gap-3"></div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter Modal -->
    <div id="filter-modal" style="display:none;" class="fixed inset-0 z-[60] items-center justify-center p-6 bg-[#F5F2EB]/40 backdrop-blur-md">
        <div class="relative z-20 w-full max-w-[630px] bg-white rounded-[28px] shadow-2xl border border-white/60 overflow-hidden flex flex-col max-h-[85vh] ink-stain">
            <div class="px-7 pt-9 pb-6 border-b border-[#D5CCB6]/15 flex flex-col items-center justify-center bg-white/50 backdrop-blur-md z-10 flex-shrink-0 relative">
                <div class="text-center w-full">
                    <h2 class="text-3xl md:text-4xl font-serif font-medium text-[#2A2724] relative inline-block">
                        Filter & Sort
                        <svg class="absolute w-[110%] h-[15px] -bottom-2 -left-[5%] text-[#8B2626] opacity-80 overflow-visible" viewBox="0 0 100 10" fill="none" preserveAspectRatio="none">
                            <path d="M0 7 Q12.5 10 25 6 T50 7 T75 6 T100 7" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" class="swoosh-path animate-draw-line"/>
                        </svg>
                    </h2>
                </div>
                <button id="close-filter-btn" class="absolute top-7 right-7 w-8 h-8 rounded-full bg-[#FDFCF9] border border-[#D5CCB6]/20 flex items-center justify-center text-[#2A2724]/50 hover:text-[#2A2724] hover:bg-[#F5F2EB] transition-all group">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 group-hover:rotate-90 transition-transform duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path></svg>
                </button>
            </div>

            <div class="px-7 py-6 overflow-y-auto modal-scrollbar relative z-0 flex-grow space-y-8 h-[500px]">
                <!-- Sort -->
                <div>
                    <div class="flex items-center justify-between mb-3">
                        <div class="flex items-center gap-2">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5 text-[#8B2626]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"></line><polyline points="19 12 12 19 5 12"></polyline></svg>
                            <h3 class="font-sans text-[0.65rem] font-semibold uppercase tracking-widest text-[#2A2724]/50">Sort Order</h3>
                        </div>
                        <button id="sort-direction-toggle" class="flex items-center gap-1.5 px-2.5 py-1 rounded-lg bg-[#FDFCF9] border border-[#D5CCB6]/20 text-[10px] font-bold uppercase tracking-wider text-[#2A2724]/60 hover:text-[#8B2626] transition-all group">
                            <span id="sort-direction-label">Descending</span>
                            <svg id="sort-direction-icon" xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M7 15l5 5 5-5M7 9l5-5 5 5"/></svg>
                        </button>
                    </div>
                    <div class="flex flex-wrap gap-2">
                        <button data-sort="most-used" class="sort-btn px-4 py-2 bg-[#8B2626] text-white font-sans text-sm font-medium rounded-full shadow-lg border border-[#8B2626] transition-all">Most Used</button>
                        <button data-sort="alphabetical" class="sort-btn px-4 py-2 bg-white text-[#2A2724]/70 font-sans text-sm font-medium rounded-full border border-[#D5CCB6]/30 hover:text-[#2A2724] transition-all shadow-sm">Alphabetical</button>
                        <button data-sort="rating" class="sort-btn px-4 py-2 bg-white text-[#2A2724]/70 font-sans text-sm font-medium rounded-full border border-[#D5CCB6]/30 hover:text-[#2A2724] transition-all shadow-sm">Rating</button>
                    </div>
                </div>

                <div class="w-full h-px bg-gradient-to-r from-transparent via-[#2A2724]/15 to-transparent"></div>

                <!-- Stars -->
                <div>
                    <div class="flex items-center justify-between mb-3">
                        <div class="flex items-center gap-2">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5 text-[#8B2626]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon></svg>
                            <h3 class="font-sans text-[0.65rem] font-semibold uppercase tracking-widest text-[#2A2724]/50">Minimum Utility</h3>
                        </div>
                        <span id="min-rating-label" class="font-['Reenie_Beanie'] text-lg text-[#8B2626]">1+ stars</span>
                    </div>
                    <div class="flex gap-3 p-3.5 bg-white border border-[#D5CCB6]/20 rounded-2xl w-fit shadow-inner" id="filter-utility-stars">
                        <button data-rating="1" class="star-filter active active:scale-95">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round">
                                <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" fill="currentColor"></path>
                            </svg>
                        </button>
                        <button data-rating="2" class="star-filter active:scale-95">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round" style="transform:rotate(15deg)">
                                <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" fill="currentColor"></path>
                            </svg>
                        </button>
                        <button data-rating="3" class="star-filter active:scale-95">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round" style="transform:rotate(-10deg)">
                                <path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" fill="currentColor"></path>
                            </svg>
                        </button>
                    </div>
                </div>

                <div class="w-full h-px bg-gradient-to-r from-transparent via-[#2A2724]/15 to-transparent"></div>

                <!-- Disciplines -->
                <div>
                    <div class="flex items-center gap-2 mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5 text-[#8B2626]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3"></polygon></svg>
                        <h3 class="font-sans text-[0.65rem] font-semibold uppercase tracking-widest text-[#2A2724]/50">Disciplines</h3>
                    </div>
                    <div id="filter-disciplines-container" class="flex flex-wrap gap-2"></div>
                </div>

                <div class="w-full h-px bg-gradient-to-r from-transparent via-[#2A2724]/15 to-transparent"></div>

                <!-- Tags -->
                <div class="pb-4">
                    <div class="flex items-center justify-between mb-4">
                        <div class="flex items-center gap-2">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5 text-[#8B2626]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path></svg>
                            <h3 class="font-sans text-[0.65rem] font-semibold uppercase tracking-widest text-[#2A2724]/50">Filter by Tags</h3>
                        </div>
                        <button id="toggle-all-tags" class="font-sans text-[10px] font-bold uppercase tracking-wider text-[#8B2626]/60 hover:text-[#8B2626] transition-colors">Select All</button>
                    </div>
                    <div id="filter-tags-container" class="flex flex-wrap gap-2"></div>
                </div>
            </div>

            <div class="p-6 border-t border-[#D5CCB6]/15 bg-[#FDFCF9] relative z-10 flex items-center justify-between flex-shrink-0">
                <button id="clear-filter-btn" class="font-['Reenie_Beanie'] text-[1.4rem] font-medium text-[#8B2626] hover:bg-[#8B2626]/10 px-4 py-1.5 rounded-lg transition-all underline underline-offset-4">Clear Filters</button>
                <button id="apply-filter-btn" class="bg-seal hover:bg-[#6D1E1E] text-white px-6 py-3 rounded-xl font-sans text-sm font-medium transition-all shadow-md hover:-translate-y-0.5 flex items-center gap-2">
                    <span>Apply Filters</span>
                    <span id="filter-result-count" class="font-['Reenie_Beanie'] text-lg text-white/80 pt-0.5">-- concepts</span>
                </button>
            </div>
        </div>
    </div>

    <!-- Bottom Bar -->
    <div class="fixed bottom-10 left-1/2 -translate-x-1/2 bg-white/80 backdrop-blur-xl border border-[#E8E2D2] rounded-[24px] shadow-[0_20px_50px_rgba(42,39,36,0.15)] p-2.5 flex items-center z-50">
        <div class="flex items-center gap-3 pl-4 pr-3 py-2 group cursor-text" onclick="document.getElementById('concept-search').focus()">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-[#A39985] group-hover:text-seal transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
            <input type="text" id="concept-search" placeholder="Search concepts" class="bg-transparent border-none outline-none font-sans text-xs font-bold text-[#2A2724] placeholder:text-[#A39985] w-32 transition-all focus:w-48">
        </div>
        <div class="w-px h-6 bg-[#E8E2D2] mx-1"></div>
        <button id="filter-btn" class="bg-seal hover:bg-seal/90 text-white rounded-xl px-5 py-2.5 text-[11px] uppercase tracking-widest font-sans font-bold transition-colors shadow-sm">Filter & Sort</button>
    </div>

    <script is:inline>
        const searchInput = document.getElementById('concept-search');
        const cards = document.querySelectorAll('.catalog-card');
        const conceptModal = document.getElementById('concept-modal');
        const filterModal = document.getElementById('filter-modal');
        const filterResultCount = document.getElementById('filter-result-count');
        const filterSortButtons = document.querySelectorAll('.sort-btn');
        const filterStarButtons = document.querySelectorAll('.star-filter');
        const masonryGrid = document.querySelector('.masonry-grid');
        const filterTagsContainer = document.getElementById('filter-tags-container');
        const filterDisciplinesContainer = document.getElementById('filter-disciplines-container');

        // Attach all button listeners
        document.getElementById('filter-btn').addEventListener('click', openFilterModal);
        document.getElementById('close-filter-btn').addEventListener('click', closeFilterModal);
        document.getElementById('apply-filter-btn').addEventListener('click', applyFilterSystem);
        document.getElementById('clear-filter-btn').addEventListener('click', clearAllFilters);
        document.getElementById('close-modal-btn').addEventListener('click', hideModal);

        let currentFilterState = {
            query: '',
            sort: 'most-used',
            sortOrder: 'desc',
            disciplines: ['All'],
            tags: [],
            minUtility: 0
        };

        // Build disciplines
        const availableDisciplines = new Set();
        cards.forEach(card => {
            const disc = card.getAttribute('data-discipline');
            if (disc) disc.split('/').forEach(d => availableDisciplines.add(d.trim()));
        });
        const sortedDisciplines = Array.from(availableDisciplines).sort();

        function renderFilterDisciplines() {
            const allActive = currentFilterState.disciplines.includes('All');
            let html = `<button data-discipline="All" class="disc-filter-btn px-4 py-2 rounded-full text-xs font-bold uppercase tracking-widest transition-all ${allActive ? 'bg-[#2A2724] text-white shadow-md' : 'bg-[#F5F2EB] text-[#2A2724]/60 border border-[#D5CCB6]/20 hover:bg-[#E8E2D2]'}">All Disciplines</button>`;
            html += sortedDisciplines.map(disc => {
                const isActive = currentFilterState.disciplines.includes(disc);
                return `<button data-discipline="${disc}" class="disc-filter-btn px-4 py-2 rounded-full text-xs font-bold uppercase tracking-widest transition-all ${isActive ? 'bg-[#8B2626] text-white shadow-md' : 'bg-[#FDFCF9] text-[#2A2724]/60 border border-[#D5CCB6]/20 hover:bg-[#F5F2EB]'}">${disc}</button>`;
            }).join('');
            filterDisciplinesContainer.innerHTML = html;
            filterDisciplinesContainer.querySelectorAll('.disc-filter-btn').forEach(btn => {
                btn.addEventListener('click', () => {
                    const disc = btn.getAttribute('data-discipline');
                    if (disc === 'All') {
                        currentFilterState.disciplines = ['All'];
                    } else {
                        currentFilterState.disciplines = currentFilterState.disciplines.filter(d => d !== 'All');
                        if (currentFilterState.disciplines.includes(disc)) {
                            currentFilterState.disciplines = currentFilterState.disciplines.filter(d => d !== disc);
                            if (currentFilterState.disciplines.length === 0) currentFilterState.disciplines = ['All'];
                        } else {
                            currentFilterState.disciplines.push(disc);
                        }
                    }
                    renderFilterDisciplines();
                    updateFilterPreview();
                });
            });
        }
        renderFilterDisciplines();

        // Build tags
        const availableTags = new Set();
        cards.forEach(card => {
            const tags = card.getAttribute('data-tags') || '';
            if (tags) tags.split(',').forEach(t => { if(t.trim()) availableTags.add(t.trim()); });
        });
        const sortedTags = Array.from(availableTags).sort();

        function renderFilterTags() {
            filterTagsContainer.innerHTML = sortedTags.map(tag => {
                const isActive = currentFilterState.tags.includes(tag);
                return `<button data-tag="${tag}" class="tag-filter-btn px-3 py-1 rounded-full text-[10px] font-semibold uppercase tracking-widest transition-all ${isActive ? 'bg-[#8B2626] text-white shadow-md' : 'bg-[#8B2626]/10 text-[#8B2626] border border-[#8B2626]/20 hover:bg-[#8B2626]/20'}">${tag}</button>`;
            }).join('');
            document.querySelectorAll('.tag-filter-btn').forEach(btn => {
                btn.addEventListener('click', () => {
                    const tag = btn.getAttribute('data-tag');
                    if (currentFilterState.tags.includes(tag)) {
                        currentFilterState.tags = currentFilterState.tags.filter(t => t !== tag);
                    } else {
                        currentFilterState.tags.push(tag);
                    }
                    renderFilterTags();
                    updateFilterPreview();
                });
            });
        }
        renderFilterTags();

        document.getElementById('toggle-all-tags').addEventListener('click', (e) => {
            if (currentFilterState.tags.length === sortedTags.length) {
                currentFilterState.tags = [];
                e.target.textContent = 'Select All';
            } else {
                currentFilterState.tags = [...sortedTags];
                e.target.textContent = 'Clear All';
            }
            renderFilterTags();
            updateFilterPreview();
        });

        filterSortButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                currentFilterState.sort = btn.getAttribute('data-sort');
                filterSortButtons.forEach(b => {
                    b.classList.remove('bg-[#8B2626]', 'text-white', 'shadow-lg', 'border-[#8B2626]');
                    b.classList.add('bg-white', 'text-[#2A2724]/70');
                });
                btn.classList.add('bg-[#8B2626]', 'text-white', 'shadow-lg');
                btn.classList.remove('bg-white', 'text-[#2A2724]/70');
                updateFilterPreview();
            });
        });

        const sortDirectionBtn = document.getElementById('sort-direction-toggle');
        const sortDirectionLabel = document.getElementById('sort-direction-label');
        const sortDirectionIcon = document.getElementById('sort-direction-icon');
        sortDirectionBtn.addEventListener('click', () => {
            currentFilterState.sortOrder = currentFilterState.sortOrder === 'desc' ? 'asc' : 'desc';
            sortDirectionLabel.textContent = currentFilterState.sortOrder === 'desc' ? 'Descending' : 'Ascending';
            sortDirectionIcon.style.transform = currentFilterState.sortOrder === 'desc' ? 'rotate(0deg)' : 'rotate(180deg)';
            updateFilterPreview();
        });

        filterStarButtons.forEach(btn => {
            btn.addEventListener('click', () => {
                const rating = parseInt(btn.getAttribute('data-rating'));
                currentFilterState.minUtility = rating;
                filterStarButtons.forEach((star, index) => {
                    if (index < rating) {
                        star.classList.add('active');
                    } else {
                        star.classList.remove('active');
                    }
                });
                document.getElementById('min-rating-label').textContent = `${rating}+ stars`;
                updateFilterPreview();
            });
        });

        function shouldShowCard(card) {
            const title = card.querySelector('.model-title').textContent.toLowerCase();
            const desc = card.querySelector('p')?.textContent.toLowerCase() || '';
            const tags = card.getAttribute('data-tags') ? card.getAttribute('data-tags').split(',').map(t => t.trim()) : [];
            const stars = parseInt(card.getAttribute('data-rating')) || 0;
            const discipline = card.getAttribute('data-discipline') || '';

            const queryMatch = !currentFilterState.query || title.includes(currentFilterState.query) || desc.includes(currentFilterState.query);
            let discMatch = currentFilterState.disciplines.includes('All');
            if (!discMatch) discMatch = currentFilterState.disciplines.some(d => discipline.includes(d));
            const tagsMatch = currentFilterState.tags.length === 0 || currentFilterState.tags.some(t => tags.includes(t));
            const starsMatch = stars >= currentFilterState.minUtility;

            return queryMatch && discMatch && tagsMatch && starsMatch;
        }

        function updateFilterPreview() {
            let count = 0;
            cards.forEach(card => { if (shouldShowCard(card)) count++; });
            filterResultCount.textContent = `${count} concepts`;
        }

        function openFilterModal() {
            filterModal.style.display = 'flex';
            document.body.style.overflow = 'hidden';
            const path = filterModal.querySelector('.swoosh-path');
            if (path) { path.style.animation = 'none'; path.offsetHeight; path.style.animation = null; }
            updateFilterPreview();
        }

        function closeFilterModal() {
            filterModal.style.display = 'none';
            if (conceptModal.style.display !== 'flex') document.body.style.overflow = '';
        }

        function applyFilterSystem() {
            cards.forEach(card => { 
                const wrapper = card.closest('.masonry-item-wrapper');
                if (wrapper) wrapper.style.display = shouldShowCard(card) ? 'block' : 'none';
            });
            sortCards();
            closeFilterModal();
            
            // Trigger masonry reflow
            requestAnimationFrame(() => {
                requestAnimationFrame(layoutMasonryConcepts);
            });
        }

        function sortCards() {
            const cardsArray = Array.from(cards);
            const isDesc = currentFilterState.sortOrder === 'desc';
            cardsArray.sort((a, b) => {
                let result = 0;
                if (currentFilterState.sort === 'alphabetical') {
                    result = a.querySelector('.model-title').textContent.trim().localeCompare(b.querySelector('.model-title').textContent.trim());
                } else if (currentFilterState.sort === 'most-used') {
                    result = parseInt(a.getAttribute('data-linked')) - parseInt(b.getAttribute('data-linked'));
                } else if (currentFilterState.sort === 'rating') {
                    result = parseInt(a.getAttribute('data-rating')) - parseInt(b.getAttribute('data-rating'));
                }
                return isDesc ? -result : result;
            });
            
            // Move WRAPPERS, not just cards
            cardsArray.forEach(card => {
                const wrapper = card.closest('.masonry-item-wrapper');
                if (wrapper) masonryGrid.appendChild(wrapper);
            });
        }

        function clearAllFilters() {
            currentFilterState = { query: '', sort: 'most-used', sortOrder: 'desc', disciplines: ['All'], tags: [], minUtility: 0 };
            renderFilterDisciplines();
            renderFilterTags();
            filterStarButtons.forEach(star => { star.classList.remove('active'); });
            document.getElementById('min-rating-label').textContent = '0+ stars';
            searchInput.value = ''; // Clear search input
            applyFilterSystem();
        }

        searchInput.addEventListener('input', (e) => {
            currentFilterState.query = e.target.value.toLowerCase().trim();
            applyFilterSystem();
        });

        // Card click → open modal
        cards.forEach(card => {
            card.addEventListener('click', () => {
                const title = card.getAttribute('data-title');
                const description = card.getAttribute('data-description');
                const discipline = card.getAttribute('data-discipline');
                const tags = card.getAttribute('data-tags');
                const origin = card.getAttribute('data-origin');
                const rating = parseInt(card.getAttribute('data-rating')) || 0;
                const linked = card.getAttribute('data-linked');
                const fieldNote = card.getAttribute('data-fieldnote');
                const notesOfUsed = card.getAttribute('data-notesofused') || '';
                const related = card.getAttribute('data-related') || '';

                // Metadata population
                const setElTxt = (id, val) => { const el = document.getElementById(id); if (el) el.textContent = val || ''; };
                const setElHtml = (id, val) => { const el = document.getElementById(id); if (el) el.innerHTML = val || ''; };
                const toggleSec = (id, show) => { const el = document.getElementById(id); if (el) el.style.display = show ? 'block' : 'none'; };
                const toggleSecFlex = (id, show) => { const el = document.getElementById(id); if (el) el.style.display = show ? 'flex' : 'none'; };

                setElTxt('modal-title', title);
                setElTxt('modal-bg-letter', title.charAt(0));
                setElTxt('modal-summary', card.getAttribute('data-short'));
                
                const bodyContent = document.getElementById(`content-${card.getAttribute('data-id')}`);
                setElHtml('modal-detailed-desc', bodyContent ? bodyContent.innerHTML : `<p>${description}</p>`);
                
                setElTxt('modal-origin', origin);
                setElTxt('modal-linked', linked);

                // Extract discipline and framework
                let displayDiscipline = discipline;
                let displayFramework = '';
                if (discipline && discipline.includes('/')) {
                    const parts = discipline.split('/');
                    displayDiscipline = parts[0].trim();
                    displayFramework = parts[1].trim();
                }

                // Ontology header section
                const discTxt = document.getElementById('modal-discipline-text');
                const frameTxt = document.getElementById('modal-framework-text');
                const ontSec = document.getElementById('modal-ontology-attribution');
                const frameCont = document.getElementById('modal-framework-container');
                const discCont = document.getElementById('modal-discipline-container');
                
                if (ontSec) ontSec.style.display = 'none';
                if (discCont) discCont.style.display = 'none';
                if (frameCont) frameCont.style.display = 'none';

                if (displayDiscipline || displayFramework) {
                    if (ontSec) ontSec.style.display = 'flex';
                    if (displayDiscipline) {
                        if (discTxt) discTxt.textContent = displayDiscipline;
                        if (discCont) discCont.style.display = 'flex';
                    }
                    if (displayFramework) {
                        if (frameTxt) frameTxt.textContent = displayFramework;
                        if (frameCont) frameCont.style.display = 'flex';
                    }
                }

                // Tags header section
                if (tags) {
                    setElTxt('modal-tags-list', tags.split(',').map(t => t.trim()).join(', '));
                    toggleSecFlex('modal-tags-attribution', true);
                } else {
                    toggleSecFlex('modal-tags-attribution', false);
                }

                // Stars
                const rotations = ['', 'transform: rotate(15deg);', 'transform: rotate(-10deg);', 'transform: rotate(5deg);', 'transform: rotate(-15deg);'];
                const starsHtml = Array.from({length: rating}).map((_, i) =>
                    `<svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" fill-opacity="0.2" stroke="#8B2626" stroke-width="2" stroke-linejoin="round" style="${rotations[i] || ''}"><path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z"></path></svg>`
                ).join('');
                setElHtml('modal-stars', starsHtml);

                // Field note (Notes of Myself)
                if (fieldNote) {
                    setElTxt('modal-field-note', fieldNote);
                    toggleSec('modal-field-note-section', true);
                } else {
                    toggleSec('modal-field-note-section', false);
                }

                // Notes of used
                if (notesOfUsed) {
                    const notes = notesOfUsed.split(',').filter(n => n.trim());
                    const nHtml = notes.map(name =>
                        `<div class="group p-3.5 bg-[#FDFCF9]/50 border border-[#D5CCB6]/40 rounded-xl flex items-center justify-between cursor-pointer hover:bg-[#FDFCF9] hover:border-[#7C6B9E]/40 hover:shadow-sm transition-all">
    <p class="font-serif text-[17px] text-[#2A2724] group-hover:text-[#7C6B9E] font-medium transition-colors">${name.trim()}</p>
    <svg class="w-4 h-4 text-[#A39985] opacity-0 group-hover:opacity-100 group-hover:text-[#7C6B9E] group-hover:translate-x-1 transition-all" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
</div>`
                    ).join('');
                    setElHtml('modal-notes-items', nHtml);
                    toggleSec('modal-notes-section', true);
                } else {
                    toggleSec('modal-notes-section', false);
                }

                // Related concepts
                if (related) {
                    const rItems = related.split(',').filter(r => r.trim());
                    const rHtml = rItems.map(name =>
    `<div class="group p-3.5 bg-[#FDFCF9]/50 border border-[#D5CCB6]/40 rounded-xl flex items-center justify-between cursor-pointer hover:bg-[#FDFCF9] hover:border-[#B08D57]/40 hover:shadow-sm transition-all">
        <p class="font-serif text-[17px] text-[#2A2724] group-hover:text-[#B08D57] font-medium transition-colors">${name.trim()}</p>
        <svg class="w-4 h-4 text-[#A39985] opacity-0 group-hover:opacity-100 group-hover:text-[#B08D57] group-hover:translate-x-1 transition-all" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
    </div>`
).join('');
                    setElHtml('modal-related-items', rHtml);
                    toggleSec('modal-related-section', true);
                } else {
                    toggleSec('modal-related-section', false);
                }

                // Attribution
                const authors = card.getAttribute('data-authors') || '';
                if (authors) {
                    setElTxt('modal-authors-list', authors.split(',').map(a => a.trim()).join(', '));
                    toggleSecFlex('modal-attribution', true);
                } else {
                    toggleSecFlex('modal-attribution', false);
                }
                // Underline animation
                const modalTitleEl = document.getElementById('modal-title');
                const modalTitleUnderline = document.getElementById('modal-title-underline');
                requestAnimationFrame(() => {
                    const rects = modalTitleEl.getClientRects();
                    const containerRect = modalTitleEl.parentElement.getBoundingClientRect();
                    if (rects.length > 0) {
                        const firstRect = rects[0];
                        modalTitleUnderline.style.width = (firstRect.width * 1.05) + 'px';
                        modalTitleUnderline.style.top = (firstRect.bottom - containerRect.top - (firstRect.height * 0.12)) + 'px';
                        modalTitleUnderline.style.left = (firstRect.left - containerRect.left - (firstRect.width * 0.025)) + 'px';
                        modalTitleUnderline.style.display = 'block';
                        const path = modalTitleUnderline.querySelector('path');
                        path.style.animation = 'none';
                        void path.offsetWidth;
                        path.style.animation = 'drawSwoosh 0.8s ease-out 0.4s forwards';
                    }
                });

		conceptModal.style.display = 'flex';
		document.body.style.overflow = 'hidden';
		requestAnimationFrame(() => {
    		conceptModal.querySelectorAll('.overflow-y-auto').forEach(el => el.scrollTop = 0);
		});
            });
        });

        function hideModal() {
            conceptModal.style.display = 'none';
            if (filterModal.style.display !== 'flex') document.body.style.overflow = '';
        }

        conceptModal.addEventListener('click', (e) => { if (e.target === conceptModal) hideModal(); });
        filterModal.addEventListener('click', (e) => { if (e.target === filterModal) closeFilterModal(); });
        document.addEventListener('keydown', (e) => { if (e.key === 'Escape') { closeFilterModal(); hideModal(); } });

        function layoutMasonryConcepts() {
            const grid = document.querySelector('.masonry-grid');
            if (!grid) return;

            const itemsArr = Array.from(grid.querySelectorAll('.masonry-item-wrapper')).filter(item => {
                // More robust check for visibility
                return item.style.display !== 'none' && (item.offsetWidth > 0 || item.offsetHeight > 0 || item.getClientRects().length > 0);
            });
            
            const containerWidth = grid.offsetWidth;
            let cols = 1;
            if (window.innerWidth >= 1024) cols = 3;
            else if (window.innerWidth >= 768) cols = 2;

            const gap = 20; // 1.25rem = 20px
            const colWidth = (containerWidth - (gap * (cols - 1))) / cols;
            const colHeights = new Array(cols).fill(0);

            itemsArr.forEach((item) => {
                const minHeight = Math.min(...colHeights);
                const colIndex = colHeights.indexOf(minHeight);
                const x = colIndex * (colWidth + gap);
                const y = minHeight;

                item.style.width = `${colWidth}px`;
                item.style.transform = `translate3d(${x}px, ${y}px, 0)`;
                
                colHeights[colIndex] += item.offsetHeight + gap;
            });

            grid.style.height = `${Math.max(...colHeights)}px`;
        }

        // Trigger masonry layout
        window.addEventListener('load', layoutMasonryConcepts);
        window.addEventListener('resize', layoutMasonryConcepts);
        
        // Initial run
        layoutMasonryConcepts();

        // Initial run
        layoutMasonryConcepts();

        updateFilterPreview();
    </script>
</body>
</html>