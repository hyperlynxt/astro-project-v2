---
interface Props {
  title: string;
  num?: string;
}

const { title, num } = Astro.props;
---

<div class="section-head">
    <h2>{title}</h2>
    {num && <span class="num">{num}</span>}
</div>
<p class="section-note"><slot /></p>
