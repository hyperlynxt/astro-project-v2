---
---
<style is:global>
    .animate-fade-in { animation: fadeIn 0.3s ease-out forwards; }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    @keyframes slideUpScale { from { opacity: 0; transform: translateY(40px) scale(0.97); } to { opacity: 1; transform: translateY(0) scale(1); } }
    @keyframes slideInRight { from { opacity: 0; transform: translateX(-40px); } to { opacity: 1; transform: translateX(0); } }
    .animate-modal-enter { opacity: 0; animation: slideUpScale 0.5s cubic-bezier(0.16, 1, 0.3, 1) 0.05s forwards; }
    .animate-content-enter { opacity: 0; animation: slideInRight 0.5s cubic-bezier(0.2, 0.8, 0.2, 1) 0.15s forwards; }
    
    .ink-stain {
        background-color: #FDFCF9;
        background-image: url("https://www.transparenttextures.com/patterns/natural-paper.png"), url("https://www.transparenttextures.com/patterns/natural-paper.png"), radial-gradient(circle at 10% 90%, rgba(42,39,36,0.03) 0%, rgba(253,252,249,0) 60%);
        background-position: 0px 0px, 40px 60px, 0 0;
        background-repeat: repeat, repeat, no-repeat;
    }
    .swoosh-path { stroke-dasharray: 100; stroke-dashoffset: 100; }
    @keyframes drawSwoosh { to { stroke-dashoffset: 0; } }

    #note-modal .modal-scrollbar::-webkit-scrollbar { width: 4px; }
    #note-modal .modal-scrollbar::-webkit-scrollbar-thumb { background-color: rgba(42, 39, 36, 0.15); border-radius: 10px; }

    #note-modal {
        position: fixed; inset: 0; z-index: 100;
        display: none; align-items: center; justify-content: center;
        padding: 2rem; background: rgba(42,39,35,0.25);
        backdrop-filter: blur(5px); -webkit-backdrop-filter: blur(5px);
    }
    .note-modal-inner {
        width: 100%; max-width: 900px; height: 85vh;
        background: white; border-radius: 40px; overflow: hidden;
        border: 1px solid rgba(255,255,255,0.6);
        box-shadow: 0 40px 100px rgba(42,39,36,0.15);
        position: relative; ring: 1px ring-[#D5CCB6]/30;
        display: flex; flex-direction: column;
    }

    .modal-footer-bar {
        padding: 1rem 2.5rem 1.5rem;
        border-top: 1px solid rgba(180,188,192,0.15);
        display: flex; align-items: center; justify-content: space-between;
        background: rgba(253,252,249,0.8);
        backdrop-blur: 8px; flex-shrink: 0;
    }

    .properties-btn {
        display: inline-flex; align-items: center; gap: 0.5rem;
        font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 700;
        letter-spacing: 0.14em; text-transform: uppercase;
        color: rgba(42,39,35,0.45); background: transparent;
        border: 1.5px solid rgba(42,39,35,0.12); border-radius: 100px;
        padding: 7px 16px; cursor: pointer; transition: all 0.2s ease;
    }
    .properties-btn:hover { color: #B4BCC0; border-color: rgba(180,188,192,0.4); background: rgba(180,188,192,0.05); }

    #note-props-overlay {
        position: absolute; inset: 0; z-index: 120;
        display: flex; align-items: flex-end; justify-content: stretch;
        background: rgba(42,39,35,0.18); backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px); border-radius: 40px;
        overflow: hidden; opacity: 0; pointer-events: none; transition: opacity 0.25s ease;
    }
    #note-props-overlay.open { opacity: 1; pointer-events: all; }
    
    .note-props-panel {
        width: 100%; background: #FDFCF9;
        background-image: url("https://www.transparenttextures.com/patterns/natural-paper.png");
        border-radius: 28px 28px 40px 40px; border-top: 1px solid rgba(180,188,192,0.2);
        box-shadow: 0 -12px 48px rgba(42,39,35,0.14);
        transform: translateY(100%); transition: transform 0.4s cubic-bezier(0.16,1,0.3,1);
        max-height: 85%; display: flex; flex-direction: column; overflow: hidden;
    }
    #note-props-overlay.open .note-props-panel { transform: translateY(0); }

    .note-props-band {
        height: 4px; border-radius: 28px 28px 0 0; flex-shrink: 0; opacity: 0.55;
        background: linear-gradient(90deg, #D5CCB6, #B4BCC0, #D5CCB6);
    }
    .note-props-header { padding: 1.2rem 2rem 1.1rem; display: flex; align-items: center; justify-content: space-between; flex-shrink: 0; }
    .note-props-eyebrow {
        font-family: 'Inter', sans-serif; font-size: 9px; font-weight: 700;
        letter-spacing: 0.16em; text-transform: uppercase; color: rgba(42,39,35,0.28); margin-bottom: 0.2rem;
    }
    .note-props-title { font-family: 'EB Garamond', serif; font-size: 1.35rem; font-weight: 500; color: #2A2724; line-height: 1.2; }
    .note-props-close {
        width: 32px; height: 32px; border-radius: 50%; background: white;
        border: 1px solid rgba(42,39,35,0.10); display: flex; align-items: center; justify-content: center;
        cursor: pointer; color: rgba(42,39,35,0.38); transition: all 0.2s; flex-shrink: 0;
    }
    .note-props-close:hover { background: #F5F2EB; color: #B4BCC0; }

    .note-props-body { overflow-y: auto; padding: 1.2rem 2rem 2.5rem; display: grid; grid-template-columns: 1fr 1fr; gap: 0; }
    .note-prop-row { padding: 1rem 0.6rem; border-bottom: 1px solid rgba(42,39,35,0.05); }
    .note-prop-row.full-width { grid-column: 1 / -1; }

    .note-prop-label {
        font-family: 'Inter', sans-serif; font-size: 9px; font-weight: 700;
        letter-spacing: 0.13em; text-transform: uppercase; color: rgba(42,39,35,0.30); margin-bottom: 0.5rem;
    }
    .note-prop-value { font-family: 'EB Garamond', serif; font-size: 1.05rem; color: rgba(42,39,35,0.78); line-height: 1.4; }
    .note-prop-value.italic-accent { color: #8C8C8C; font-style: italic; }
    
    .dot-rating { display: flex; gap: 5px; align-items: center; }
    .dot { width: 8px; height: 8px; border-radius: 50%; background: #F5F2EB; border: 1.5px solid #D5CCB6; }
    .dot.filled { background: #B4BCC0; border-color: #B4BCC0; }

    .note-prop-chips { display: flex; flex-wrap: wrap; gap: 8px; }
    .note-prop-chip {
        display: inline-block; font-family: 'Inter', sans-serif; font-size: 9px; font-weight: 700;
        letter-spacing: 0.09em; text-transform: uppercase; padding: 4px 12px; border-radius: 8px;
        background: rgba(42,39,35,0.06); color: rgba(42,39,35,0.5); border: 1px solid rgba(42,39,35,0.08);
    }

    #note-modal strong, 
    #note-modal b {
        color: #8C8C8C !important;
        font-weight: 600 !important;
    }

    #note-modal h2, 
    #note-modal h3 {
        font-family: 'EB Garamond', serif; font-size: 1.7rem; font-weight: 500; color: #111827;
        margin-top: 48px; margin-bottom: 24px; padding-bottom: 12px;
        border-bottom: 1px solid #E6DFD1; line-height: 1.2;
    }
</style>

<div id="note-modal">
    <div class="note-modal-inner animate-modal-enter">
        <div class="flex-1 flex flex-col relative bg-[#FDFCF9] ink-stain overflow-y-auto modal-scrollbar z-10 p-6 md:p-12 lg:p-16">
            <button id="close-note-modal-btn" class="absolute top-8 right-8 w-12 h-12 rounded-full bg-white/80 backdrop-blur-md border border-[#D5CCB6] shadow-sm hover:bg-white flex items-center justify-center text-[#2A2724] transition-all group z-[60]">
                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 opacity-60 group-hover:opacity-100 group-hover:rotate-90 transition-all duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path></svg>
            </button>

            <div class="relative animate-content-enter">
                <div id="modal-note-bg-letter" class="absolute top-0 right-0 text-[24rem] font-serif font-bold text-[#2A2724]/[0.015] leading-none pointer-events-none select-none -z-10 -rotate-3"></div>

                <div class="mb-14 text-center flex flex-col items-center">
                    <div id="modal-note-stars" class="flex gap-1 text-[#B4BCC0] mb-6"></div>
                    <p id="modal-note-source" class="font-sans text-xs font-bold uppercase tracking-[0.2em] text-[#B4BCC0] opacity-80 mb-2"></p>
                    <h1 class="font-serif text-4xl md:text-5xl lg:text-[4.5rem] font-medium text-[#2A2724] leading-tight mb-8 tracking-tight relative inline-block mx-auto">
                        <span id="modal-note-title" class="inline"></span>
                        <svg id="modal-note-underline" class="absolute h-[12px] text-[#B4BCC0] opacity-70 overflow-visible pointer-events-none" viewBox="0 0 100 10" fill="none" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none" style="display: none;">
                            <path d="M0,8 C25,12 75,4 100,8" stroke="currentColor" stroke-width="2" stroke-linecap="round" class="swoosh-path"></path>
                        </svg>
                    </h1>
                    <div class="max-w-3xl mx-auto">
                        <div id="modal-note-summary" class="font-serif text-[1.25rem] md:text-[1.4rem] leading-relaxed text-[#2A2724]"></div>
                    </div>
                </div>

                <div id="modal-note-body" class="font-serif text-xl md:text-[1.45rem] leading-[1.85] text-[#2A2724]/85 space-y-8 max-w-[900px] z-10 relative mb-20 prose prose-ink"></div>
            </div>
        </div>

        <footer class="modal-footer-bar">
            <button id="open-note-props-btn" class="properties-btn">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="4" y1="21" x2="4" y2="14"></line><line x1="4" y1="10" x2="4" y2="3"></line><line x1="12" y1="21" x2="12" y2="12"></line><line x1="12" y1="8" x2="12" y2="3"></line><line x1="20" y1="21" x2="20" y2="16"></line><line x1="20" y1="12" x2="20" y2="3"></line><line x1="1" y1="14" x2="7" y2="14"></line><line x1="9" y1="8" x2="15" y2="8"></line><line x1="17" y1="16" x2="23" y2="16"></line></svg>
                Research Properties
            </button>
        </footer>

        <div id="note-props-overlay">
            <div class="note-props-panel">
                <div class="note-props-band"></div>
                <div class="note-props-header">
                    <div>
                        <p class="note-props-eyebrow">Properties</p>
                        <h3 id="note-props-title" class="note-props-title"></h3>
                    </div>
                    <button id="close-note-props-btn" class="note-props-close">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
                    </button>
                </div>
                <div id="note-props-body" class="note-props-body"></div>
            </div>
        </div>
    </div>
</div>

<script is:inline>
    (function() {
        function dots(n) {
            let h = '';
            for(let i=1; i<=3; i++) h += `<span class="dot${i <= n ? ' filled' : ''}"></span>`;
            return h;
        }

        function buildProps(d) {
            const body = document.getElementById('note-props-body');
            const title = document.getElementById('note-props-title');
            body.innerHTML = '';
            title.textContent = d.title;

            const rows = [
                { label: 'Rating', html: `<div class="dot-rating">${dots(parseInt(d.rating)||0)}</div>` },
                { label: 'Date', html: `<span class="note-prop-value italic-accent">${new Date(d.date).toLocaleDateString('en-US', { year:'numeric', month:'long', day:'numeric'})}</span>` },
                { label: 'Source', html: `<span class="note-prop-value">${d.source || 'General Research'}</span>` },
                { 
                    label: 'Tags', 
                    html: (d.tags) ? `<div class="note-prop-chips">${(Array.isArray(d.tags) ? d.tags : d.tags.split(',')).map(t => `<span class="note-prop-chip">${t.trim()}</span>`).join('')}</div>` : '<span class="note-prop-value">—</span>',
                    full: true 
                }
            ];

            rows.forEach(row => {
                const div = document.createElement('div');
                div.className = 'note-prop-row';
                if (row.full) div.classList.add('full-width');
                div.innerHTML = `<p class="note-prop-label">${row.label}</p><div>${row.html}</div>`;
                body.appendChild(div);
            });
            document.getElementById('note-props-overlay').classList.add('open');
        }

        window.openNoteModal = function(id, source, bodyHtml) {
            const modal = document.getElementById('note-modal');
            const d = source.find(c => c.id === id);
            if (!d) return;

            document.getElementById('modal-note-title').textContent = d.title;
            document.getElementById('modal-note-bg-letter').textContent = d.title[0].toUpperCase();
            document.getElementById('modal-note-source').textContent = d.source || 'General Research';
            
            const starHtml = Array.from({ length: parseInt(d.rating) || 0 }).map((_, i) => 
                `<svg width="22" height="22" viewBox="0 0 24 24" fill="#B4BCC0" fill-opacity="0.3" stroke="#B4BCC0" stroke-width="2" stroke-linejoin="round" style="transform: rotate(${(i % 2 === 0 ? 10 : -10)}deg)"><path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z"/></svg>`
            ).join('');
            document.getElementById('modal-note-stars').innerHTML = starHtml;
            
            document.getElementById('modal-note-summary').innerHTML = (d.summary || d.description || '').replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>');
            document.getElementById('modal-note-body').innerHTML = bodyHtml || '';

            document.getElementById('open-note-props-btn').onclick = () => buildProps(d);

            modal.style.display = 'flex';
            document.body.style.overflow = 'hidden';

            // Underline
            const titleEl = document.getElementById('modal-note-title');
            const underline = document.getElementById('modal-note-underline');
            requestAnimationFrame(() => {
                const rects = titleEl.getClientRects();
                const containerRect = titleEl.parentElement.getBoundingClientRect();
                if (rects.length > 0) {
                    const r = rects[0];
                    underline.style.width = (r.width * 1.05) + 'px';
                    underline.style.top = (r.bottom - containerRect.top - (r.height * 0.12)) + 'px';
                    underline.style.left = (r.left - containerRect.left - (r.width * 0.025)) + 'px';
                    underline.style.display = 'block';
                    const path = underline.querySelector('path');
                    if (path) { path.style.animation = 'none'; void path.offsetWidth; path.style.animation = 'drawSwoosh 0.8s ease-out 0.4s forwards'; }
                }
            });
        };

        const hide = () => {
            document.getElementById('note-modal').style.display = 'none';
            document.getElementById('note-props-overlay').classList.remove('open');
            document.body.style.overflow = '';
        };

        document.getElementById('close-note-modal-btn').onclick = hide;
        document.getElementById('close-note-props-btn').onclick = () => document.getElementById('note-props-overlay').classList.remove('open');
        document.getElementById('note-modal').onclick = e => { if(e.target === document.getElementById('note-modal')) hide(); };
        document.getElementById('note-props-overlay').onclick = e => { if(e.target === document.getElementById('note-props-overlay')) document.getElementById('note-props-overlay').classList.remove('open'); };
    })();
</script>
